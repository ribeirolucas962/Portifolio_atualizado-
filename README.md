### Comandos git

``` bash
# adicionar os arquivos modificado no git
git add.

# comitar as alterações
git commit -m "save"

# Caso o repositorio já esteja no github (opcional)
git push origin master


# Caso queira retornar a ultima versão salva
# Só funciona para as alterações que não foram comitadas
git checkout .
```